build:
  docker:
    web: api/Dockerfile.heroku
run:
  web: |
    /function/bin/heroku_run.sh
setup:
  addons:
    - plan: heroku-postgresql
      as: SQLALCHEMY_DATABASE_URI
  config:
    FASTAPI_SECRET_KEY: "e8cb82f5-b966-4496-b96e-8c021892e265"
